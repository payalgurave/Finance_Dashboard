version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - cd finance-backend
      - npm install

  build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '**/*'
  base-directory: finance-backend
  discard-paths: no
